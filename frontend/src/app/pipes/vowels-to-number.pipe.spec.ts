import { VowelsToNumberPipe } from './vowels-to-number.pipe';

describe('VowelsToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VowelsToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
